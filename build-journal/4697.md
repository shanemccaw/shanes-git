# Build Journal — #4697

- **Status:** ⏳ IN FLIGHT 2026-09-18T18:05:00Z — code complete, pushed, verified against real GitHub; DEPLOY + live confirmation pending Shane (see below)
- **Scope:** shanes-git MCP server (tracking issue shanemccaw/Shane-McCaw-MSP#4697, sub-issue of Feature #4692)
- **Repo:** shanemccaw/shanes-git (the live GitHub MCP server — real work lands here)

## Task

Three real changes to the live, in-use MCP server:
1. Remove `Done` from `move_to_status`'s enum (and, by shared-vocabulary consistency, `list_board_column`'s — see plan Decision B).
2. Add real additive/subtractive label tools (`add_label` / `remove_label`) — true delta, never `update_issue`'s dangerous full-replace.
3. Extend `close_issue` to deterministically strip terminal-state labels on close — unconditional (plan Decision A).

Plan + walkthrough: `build-journal/4697-plan.md` (written before any code change, per #4307).

## Log

- 2026-09-18T17:40:00Z — ⏳ IN FLIGHT. Cloned shanes-git, confirmed real current tool schemas (`move-to-status.ts`, `update-issue.ts`, `close-issue.ts`, `list-board-column.ts`), read parent Feature #4692 for the real label decisions. Plan doc written. Starting implementation.
- 2026-09-18T18:05:00Z — Code complete + typechecked + verified against real GitHub + pushed to `origin/main` (`651bca6`). The **live Replit deploy + live-instance confirmation remain**, and are Shane's manual action — see Verification Results. Not writing ✅ DONE (per this issue's explicit "confirm the deployed live instance reflects the change before calling this DONE" rule); tracking issue #4697 left open with `Shane To-Do`.

## Changes Made

Commit `651bca6` (code) on top of `eb84960` (plan + this bookend):

- **`src/tools/move-to-status.ts`** — removed `"Done": "0003ae3b"` from `STATUS_OPTION_ID`. `ALLOWED_STATUSES`, the tool's `enum`, its description, and `moveIssueToStatus`'s validation all derive from that record, so `move_to_status` now rejects `"Done"` with the existing clear "must be one of: …" error and makes no GitHub call. `batch_move_to_status` inherits the fix (it calls `moveIssueToStatus`).
- **`src/tools/list-board-column.ts`** — removed `"Done"` from the hardcoded `ALLOWED_STATUSES` (plan Decision B — kept in sync with `move_to_status`; the two share one vocabulary by design).
- **`src/github.ts`** — added `getIssueLabels`, `addIssueLabel` (`POST /issues/{n}/labels`, GitHub-native additive), `removeIssueLabel` (`DELETE /issues/{n}/labels/{name}`, 404 = clean idempotent no-op).
- **`src/tools/labels.ts`** (new) — `add_label` / `remove_label` write tools. Both read current labels first and change only the one-label delta (`changed: false` no-op when already present/absent); require `context` (#3538); accept optional `repo` (#3580). `update_issue`'s full-replace `labels` left untouched.
- **`src/tools/close-issue.ts`** — added `TERMINAL_STATE_LABELS = ["blocked", "in-flight", "complete"]` and, after the successful close, an unconditional allowlist-only strip of any present terminal-state label (never `Shane To-Do`/`bug`/`security`/anything else). Added `strippedLabels: string[]` to `CloseOneIssueResult` and documented it in the tool description. `batch_close_issues` inherits it.
- **`src/tools/index.ts`** — registered `addLabelTool` + `removeLabelTool`.
- **`README.md`** — documented the two new tools + the close-strip behavior; dropped `Done` from the `move_to_status`/`list_board_column` docs and the #3538 write-tool list.
- **`scripts/verify-labels-and-status.mjs`** (new) + `package.json` `verify-labels` script — real end-to-end verification harness.
- **`.gitignore`** (new) — the repo had none; added `node_modules/`, `.env.local`, `.env`, `*.log`.

## Verification Results

**`npm run typecheck` — clean (exit 0).**

**`npm run verify-labels` — ALL CHECKS PASSED against the REAL GitHub API** (`shanemccaw/Shane-McCaw-MSP`), real disposable test issue #4703 created + closed by the script:
- `add_label`/`remove_label` present in the manifest; `move_to_status` enum is exactly `["Batter Up","Backlog","AI Batter Up","Ask Shane"]` (no `Done`); `list_board_column` enum likewise; exported `ALLOWED_STATUSES` has no `Done`.
- `move_to_status({status:"Done"})` throws a clear "must be one of …" rejection whose allowed-list does NOT contain `Done` — a real clean rejection, not a silent no-op, with no GitHub call made.
- `add_label("blocked")` → `changed:true`, present; re-add → `changed:false` no-op. `remove_label("in-flight")` → `changed:true`, gone, every other label untouched; re-remove → `changed:false`. Real GitHub state confirmed directly (not just the tools' own reports).
- `close_issue(#4703, "completed")` on an issue carrying `blocked`+`Shane To-Do`+`bug` → `strippedLabels === ["blocked"]`; real GitHub state confirms `blocked` gone and `Shane To-Do`+`bug` survived; issue genuinely closed.
- PAT never appears in any result.

**DEPLOY — NOT self-confirmed; Shane's action.** The change is committed and pushed to `origin/main`
(`651bca6`), but the live instance at `https://shanes-git.replit.app` serves from a Replit
deployment. I confirmed the live instance is up (`GET /mcp` → `patConfigured:true`, `GET /healthz`
→ ok), but its `tools/list` requires a bearer token valid in the **deployed** instance's own
database, which I do not have and cannot mint without touching the Replit/production DB
(out of scope). Triggering the Replit deploy itself is a manual action reserved to Shane (agents
never trigger Replit/Staging deploys). **So this build is complete to the deploy boundary; the
Replit deploy + a live `tools/list` confirmation are the one remaining step, and they are Shane's.**

## Shane To-Do (the one remaining action)

Deploy `shanes-git` to Replit (republish `https://shanes-git.replit.app` from `origin/main` @ `651bca6`),
then a quick live confirm: `tools/list` on the deployed instance shows `add_label`/`remove_label`
and a `move_to_status` whose `status` enum has no `Done`. Once live, `#4697` can be closed.
